import React from 'react';

import {Player} from '../../components/AudioPlayer/Player';

export default {
    title: 'Audio Player',
    component: Player
};

const Template = (args) => <Player />;

export const Player1 = Template.bind({});